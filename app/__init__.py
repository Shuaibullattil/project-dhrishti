"""
Application package for high-level FastAPI components (routers, services, etc.).

This package is intentionally thin and only contains orchestration code that
builds on top of the existing detection and aggregation pipeline in `apis` and
`crowd_analysis`.
"""

